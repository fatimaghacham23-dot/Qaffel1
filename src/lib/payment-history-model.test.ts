import{describe,expect,it}from"vitest";import{normalizeHistory,normalizedPage}from"@/lib/payment-history-model";
describe("payment history model",()=>{const base={invoice_id:"i",uploaded_at:"2026-01-01",amount_usd:10};it("maps proof states and manual linkage",()=>{const rows=[{...base,id:"a",status:"pending"},{...base,id:"b",status:"accepted"},{...base,id:"c",status:"rejected"},{...base,id:"m",status:"accepted"},{...base,id:"v",status:"accepted",voided_at:"2026-02-01"}];expect(normalizeHistory(rows,new Set(["m"])).map(x=>x.type)).toEqual(["payment_voided","proof_submitted","payment_approved","proof_rejected","manual_payment_recorded"])});it("sorts, caps, and normalizes pages",()=>{expect(normalizeHistory([{...base,id:"1"},{...base,id:"2",uploaded_at:"2026-02-01"}],new Set(),1)[0].id).toBe("proof:2");expect(normalizedPage("-2")).toBe(1);expect(normalizedPage("bad")).toBe(1)});});

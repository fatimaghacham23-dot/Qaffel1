import type { ReactNode } from "react";
import Link from "next/link";
import { Breadcrumbs } from "@/components/layout/Breadcrumbs";

export function PageHeader({ title, description, badge, eyebrow, actions, mobileActions, backHref, breadcrumbs }: { title: string; description?: string; badge?: ReactNode; eyebrow?: string; actions?: ReactNode; mobileActions?: ReactNode; backHref?: string; breadcrumbs?: { label: string; href?: string }[] }) {
  return <header className="mb-6 flex min-w-0 flex-col gap-4 sm:mb-8 sm:flex-row sm:items-start sm:justify-between"><div className="min-w-0">{breadcrumbs ? <Breadcrumbs items={breadcrumbs} /> : null}<div className="flex flex-wrap items-center gap-2">{backHref ? <Link aria-label={`Back to ${backHref.slice(1).replace(/-/g, " ")}`} className="inline-flex min-h-11 items-center rounded-lg px-1 text-sm font-semibold text-cedar hover:text-cedar/80 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cedar focus-visible:ring-offset-2" href={backHref}>← Back to {backHref.slice(1).replace(/-/g, " ")}</Link> : null}{eyebrow ? <p className="q-section-label">{eyebrow}</p> : null}{badge}</div><h1 className="mt-2 break-words text-3xl font-semibold tracking-tight text-ink sm:text-4xl">{title}</h1>{description ? <p className="mt-2 max-w-2xl text-sm leading-relaxed text-slate-600 sm:text-base">{description}</p> : null}</div>{actions ? <div className="hidden shrink-0 sm:block">{actions}</div> : null}{mobileActions ? <div className="sm:hidden">{mobileActions}</div> : actions ? <div className="sm:hidden">{actions}</div> : null}</header>;
}
